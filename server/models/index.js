const User = require('./User');
const Plant = require('./Plant');
const Post = require('./Post');
const Comment = require('./Comment');
const Sale = require('./Sale')

module.exports = { User, Plant, Post, Comment, Sale };
